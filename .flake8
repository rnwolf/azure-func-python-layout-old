[flake8]
ignore = S101 # Asset detected. Bad in production, but required for testing.
exclude =
    # No need to traverse our git directory
    .git,
    # No need to check our github workflows
    .github
    # There's no value in checking cache directories
    __pycache__,
    # This virtualenv
   .venv 
max-complexity = 10